(function () {
    'use strict';
   
    //Angular Controller Definition Reference1 and Dependency Should be Injected in Array Notation
    /*angular.module("m1cp.core").controller("SampleController1",["$scope", function ($scope) {
    }]);

    //Angular Controller Definition Reference2 and Dependency Should be Injected Using $inject
    angular.module("m1cp.core").controller("SampleController2", SampleController2);
    SampleController2.$inject = ['footerService'];
    function SampleController2(footerService) {
        var vm = this;
        vm.footerlinks = footerService.getFooters();
        vm.getfooters = getfooters;
        vm.setfooters = setfooters;

        function getfooters() {

        }

        function setfooters() {

        }
    }*/
})();
